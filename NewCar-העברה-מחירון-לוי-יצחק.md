# NewCar — תוספת מחירון לוי יצחק | מסמך העברה

> **מסמך זה נועד להעברה ל-AI אחר לצורך המשך פיתוח / בדיקה.**
> נכתב על ידי Kimi בסיום שלב פיתוח: אינטגרציית מחירון לוי יצחק במסך "בדיקת רכב".
> ממשיך ישירות את מסמך ההעברה של Claude (26 באוגוסט 2026) — כל מה שכתוב שם עדיין תקף.

> ⚠️ **הקובץ מכיל פרטים טכניים רגישים של מערכת חיה (כולל פרטי API של צד שלישי).** אין לשתף אותו בפומבי, להעלות ל-GitHub או לצרף למקום ציבורי.

**תאריך הפקה:** 26 באוגוסט 2026
**בעל המערכת:** מתן — "ניו קאר חדרה", סוחר רכב יד שנייה
**כתובת האתר החי:** https://newcar-production.up.railway.app

---

## 1. מה נוסף בשלב הזה

**בדיקת מחירון לוי יצחק אוטומטית במסך "בדיקת רכב".**

הבעלים מקליד מספר רכב → המערכת מושכת את נתוני משרד התחבורה (כמו קודם) **וגם** את מחיר המחירון מלוי יצחק → המחיר מוצג בכרטיס כחול בולט בתוך תוצאות הבדיקה.

דוגמה אמיתית שאומתה באתר החי: רכב `60541402` → פולקסווגן טיגואן 2021 → **₪106,800**.

---

## 2. ההחלטה הארכיטקטונית המרכזית

הבקשה המקורית הייתה "אוטומציה שמקלידה באפליקציית לוי יצחק ומצלמת את התוצאה". **לא עשינו את זה**, מסיבות:

- אמולציית הקלדה באפליקציה (Appium/אמולטור) איטית, שבירה, ודורשת טלפון/אמולטור שרץ 24/7.
- במקום זה: פירקנו את ה-APK של האפליקציה הרשמית (`com.levinew.app` בגוגל פליי) וגילינו שהיא אפליקציית Ionic/Cordova שעובדת מול שרת API רגיל. **המערכת מדברת עם אותו שרת ישירות, בדיוק כמו שהאפליקציה עושה.** יציב, מהיר (שניה אחת), ורץ בענן בלי תלות במכשיר.

### שרת ה-API של לוי יצחק

```
Base URL:  https://s.leviitzhak.xyz
חובה Header:  Origin: https://levi-itzhak.co.il   (בלעדיו השרת מחזיר "Origin required")
אימות:     Authorization: Bearer <JWT>
```

| נקודת קצה | מתודה | תפקיד |
|---|---|---|
| `/users/send-validation-sms` | POST `{phone, uuid}` | שלב 1 התחברות: שולח קוד SMS לטלפון |
| `/users/validate-sms` | POST `{uuid, phone, code}` | שלב 2: מחזיר JWT (ב-`response.data`, מחרוזת או `{token}`) |
| `/main/new-get-by-licence-plate/` | POST `{plate: Number, isMotorcycleSearch: false}` | **חיפוש לפי מספר רכב — מחזיר הכול כולל מחיר מחירון** |
| `/main/get-submodel/` | POST `{type, manufacturer, model, subModel}` | פרטי דגם + מחירים לפי שנה |
| `/main/get-price/` | POST `{kod, year, isau, owners, km, aliya, addons...}` | חישוב מחיר מותאם (ק"מ/בעלויות/תוספות) — **לא מומש עדיין, ראה סעיף 5** |
| `/users/generate-test-token` | POST | מנגנון QA מוטמע באפליקציה — שימש רק לבדיקות פיתוח, **לא נמצא בקוד הפרודקשן** |

### מבנה התשובה של חיפוש לפי לוחית (החשוב ביותר)

`POST /main/new-get-by-licence-plate/` מחזיר:

```json
{
  "status": 1,
  "data": {
    "search": { "type": {...}, "manufacturer": {...}, "model": {...}, "subModel": {...}, "plate": 60541402 },
    "car": {
      "name": "פולקסווגן טיגואן LIFE L/F 150 כ\"ס (1500) אוטו'  .",
      "manufacturerName": "פולקסווגן",
      "price": 106800,            // ← מחיר המחירון
      "singlePrice": 106800,
      "year": ["2021"],
      "engineVolume": 1500,
      "ramatGimur": ["LIFE"],
      "kmInfo": {
        "last_test_km": 79604,
        "color": "שנהב לבן",
        "vin": "WVGZZZ5NZMW541516",
        "current_owner": "סוחר"
      },
      "ownershipHistory": [
        {"date": "08/2021", "type": "פרטי"},
        {"date": "09/2022", "type": "פרטי"},
        {"date": "04/2026", "type": "סוחר"}
      ],
      "aliyaDate": "2021-8"
    }
  }
}
```

**שים לב:** `price` / `singlePrice` הוא **מחיר מחירון בסיסי** בלבד — לפני התאמות ק"מ, בעלויות ותוספות. זה מסומן במפורש בכרטיס בממשק.

### מלכודות טכניות (עלו זמן, חשוב לזכור)

1. **חובה `Origin` header** בכל קריאה — בלעדיו: `{"error":"Origin required."}`.
2. **`plate` חייב להיות Number, לא מחרוזת** — אותה מלכודת כמו ב-data.gov.il.
3. **בלי טוקן:** `{"error":"Unauthorized access."}` — הטוקן הוא JWT, פיענוח ה-`exp` מראה שטוקן אמיתי (מ-SMS) תקף **כחודשיים**; הטוקן נשמר ב-SQLite והשרת בודק תוקף עם מרווח ביטחון של 10 דקות.
4. התשובות בעברית ב-UTF-8 — ב-Windows להיזהר מקריאת קבצי JSON בקידוד cp1252.

---

## 3. מה שונה בקוד

שני קבצים בלבד (גיבוי מלא של שניהם נמצא אצל הבעלים בתיקיית העבודה של השיחה):

### `server.js` — בלוק חדש "==================== מחירון לוי יצחק ===================="

ממוקם מיד לפני `app.get('/api/vehicle/:vin' ...)`. מכיל:

- **קבועים:** `LEVI_API_BASE`, `LEVI_API_ORIGIN`.
- **טבלת SQLite חדשה `levi_auth`** (שורה אחת, `id=1`): שומרת `token`, `phone`, `expiresAt`, `savedAt`. בזמן שליחת קוד SMS השדה `token` מחזיק זמנית `uuid:<uuid>` — הפונקציה `getLeviToken()` מתעלמת מערכים עם הקידומת הזו.
- **`leviTokenExpiry(token)`** — מפענח exp מה-JWT (base64url) בלי ספריות.
- **`getLeviToken()`** — מחזיר טוקן רק אם בתוקף (מרווח 10 דקות).
- **`saveLeviToken(token, phone)`** — UPSERT ל-`levi_auth`.
- **`leviApiCall(path, body, token)`** — עטיפת fetch עם ה-headers הנדרשים.
- **`POST /api/levi/auth/send-code`** (admin בלבד) — ולידציה `05XXXXXXXX`, שולח SMS דרך השרת שלהם, שומר uuid זמני.
- **`POST /api/levi/auth/verify-code`** (admin בלבד) — מאמת קוד בן 6 ספרות, שומר JWT. מיפוי סטטוסים שלהם: `1`=הצלחה, `2`=קוד שגוי, אחר=פג תוקף.
- **`GET /api/levi/auth/status`** — `{connected, phone, expiresAt}`.
- **`GET /api/levi/:plate`** — ה-endpoint הראשי. ולידציה 7-8 ספרות, 401+`needsAuth:true` אם אין טוקן, מנקה טוקן שפג אם השרת שלהם מחזיר "Unauthorized access.", ומחזיר `{success, data: {price, name, years, lastTestKm, ownershipHistory, ...}}`.

### `car-reception.html`

- **CSS חדש:** `.levi-price-card` (גרדיאנט כחול), `.levi-price-title/model/value/note`, `.levi-status-box` (קרם, למצבי שגיאה/אימות).
- **ב-`renderCheckResults`:** נוסף `<div id="leviPriceSection"></div>` בין גריד התוצאות להיסטוריית הידיים, וקריאה ל-`renderLeviPrice(plateNumber)` (אסינכרוני, לא חוסם את שאר התצוגה).
- **`renderLeviPrice(plateNumber)`** — שלושה מצבים: טוען → כרטיס מחיר (`₪` מפורמט `toLocaleString('he-IL')`) → תיבת סטטוס צהובה בשגיאה/אם הרכב לא במחירון.
- **`renderLeviAuthBox(container, plateNumber)`** — אם `needsAuth`: לעובד מוצג "פנה לבעלים"; לבעלים (`currentUser.canDelete`) מוצג טופס דו-שלבי (טלפון → קוד SMS) שמדבר עם נקודות ה-auth בשרת. אחרי אימות מוצלח קורא שוב ל-`renderLeviPrice` והמחיר מופיע מיד.

---

## 4. מצב נוכחי בפרודקשן (נכון ל-26/08/2026, אומת חיה)

- ✅ נפרס ל-Railway (`railway up --detach` מתיקיית `C:\Users\matanatedgi\NewCar`).
- ✅ האתר חי ותקין, המלאי (44 רכבים) והמסכים האחרים לא נפגעו.
- ✅ החיבור למחירון **פעיל**: `/api/levi/auth/status` מחזיר `connected: true`, טלפון `0529772134` (הטלפון של הבעלים — האימות בוצע דרך הטופס בממשק), **טוקן בתוקף עד 25/10/2026**.
- ✅ אומת מקצה לקצה בדפדפן על האתר החי: בדיקת `60541402` הציגה כרטיס "💰 מחירון לוי יצחק — ₪106,800".

**מה יקרה כשהטוקן יפוג (~25/10/2026):** בבדיקת רכב הבאה יוצג לבעלים אוטומטית טופס האימות (טלפון → קוד SMS), 20 שניות של פעולה אחת והחיבור חוזר. העובד יראה הודעה לפנות לבעלים.

---

## 5. מה לא מומש (פערים ידועים בשלב זה)

1. **חישוב מחיר מותאם** (`/main/get-price/` עם ק"מ/בעלויות/תוספות) — ה-endpoint קיים אצלם אבל החתימה המדויקת של הפרמטרים עוד לא פוצחה (ניסיונות החזירו שגיאת שרת פנימית `RetCode: -1`). כרגע מוצג מחיר בסיסי בלבד, וזה מסומן בממשק. למימוש: ללכוד בקשה אמיתית מהאפליקציה (mitmproxy) או לנסח לפי ה-XML שהם שולחים (`<Criteria><car>...`).
2. **שילוב המחירון במלאי** — הפער שהוצע במסמך הקודם ("שדה מחיר מחירון, פער מול מחיר קנייה, סימון צבעוני") עדיין פתוח; עכשיו, עם `/api/levi/:plate` עובד, המימוש פשוט.
3. **אין caching** של תוצאות מחירון — כל בדיקה פונה לשרת שלהם. נדיר שיהיה עומס, אבל אם יהיו חסימות קצב — להוסיף cache של כמה שעות לפי `plate`.
4. **תלות בצד שלישי לא-רשמי:** ה-API של לוי יצחק אינו מתועד/מוסדר. אם ישנו משהו (כתובת, שמות שדות, מנגנון אימות) — הלוגיקה מרוכזת בבלוק אחד ב-`server.js` וקלה לעדכון.

---

## 6. איך לבדוק / להריץ

```bash
# הרצה מקומית (דורש .env כמו קודם):
npm install
node server.js     # http://localhost:3001

# בדיקת מצב החיבור למחירון:
curl -u newcar:<PASSWORD> https://newcar-production.up.railway.app/api/levi/auth/status

# בדיקת מחירון לרכב:
curl -u newcar:<PASSWORD> https://newcar-production.up.railway.app/api/levi/60541402

# פריסה:
railway up --detach    # מתיקיית C:\Users\matanatedgi\NewCar
```

**בדיקת רגרסיה מהירה אחרי כל שינוי:**
1. פתח את האתר → מסך "בדיקת רכב" → הקלד `60541402` → ודא שמופיעים גם נתוני משרד התחבורה וגם כרטיס המחירון הכחול.
2. ודא שמסכי מלאי/ארכיון/הוספה עובדים כרגיל (השינוי לא נוגע בהם, אבל הקובץ משותף).
3. `node --check server.js` לפני כל פריסה.

---

## 7. הערת אבטחה/תקינה

- פרטי הכניסה לאתר (Basic Auth) לא שונו — נשארו ב-`.env` כפי שהיו.
- הטוקן של לוי יצחק שייך לחשבון האפליקציה של הבעלים (הטלפון שלו) — הוא שקול לסיסמה. הוא נשמר רק ב-SQLite על ה-volume הפרטי ב-Railway, ואין להוציא אותו לשום מקום אחר.
- נקודות ה-auth (`send-code`/`verify-code`) מוגנות ב-`requireAdmin` — העובד לא יכול לאתחל את החיבור.
