SITE_USERNAME=newcar
SITE_PASSWORD=change-this-password

# Optional discovery: configure privately on the server, never in the browser.
# Archive storage, images, imports and local evidence work without this key.
OPENAI_API_KEY=
ARCHIVE_SEARCH_MODEL=gpt-5.5
# Optional absolute path on a persistent volume. Defaults next to DB_PATH.
# VEHICLE_ARCHIVE_DB_PATH=/data/vehicle-archive.db
