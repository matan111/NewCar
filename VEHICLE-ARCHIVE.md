# NewCar — ארכיון היסטוריית רכב, שלב ראשון

## מה נבנה

- מסך מוגן לבעלים: `/vehicle-history`, עם קישור בתפריט ובתוצאות בדיקת רכב.
- תיק לפי מספר רישוי בן 7–8 ספרות; אין מגבלת שנת 2017.
- ציר זמן הכולל מקור, ק״מ, מחיר, מלל, תאריך המקור ותאריך השמירה בארכיון.
- שמירת גרסאות: אותו קישור לאחר נרמול נשמר פעם אחת כשאין שינוי. שינוי, כולל חזרה לערך קודם, יוצר גרסה נוספת.
- הפרדה בין ק״מ מטסט להצהרת מוכר; התראה על ירידה בדיווחים מתוארכים. תאריכים חסרים ותצפית בתוכנה אינם ראיה למועד מדידה.
- העלאת תמונות JPEG/PNG/WebP עד 8MB, עד 20 לרשומה, עם בדיקת חתימת קובץ וגישה לבעלים בלבד.
- ייבוא רשומות JSON וייצוא התיק לעיון. ייצוא כולל קישורים פנימיים לתמונות ואינו גיבוי מלא של הקבצים.
- צבירת ק״מ נוכחי מהמלאי אחת לשעה ובתחילת ההפעלה; שמירת נתונים ממשלתיים שנשלפו בבדיקות. מתבצע ייבוא של תצפיות קודמות מ־km_history.
- חיבור אופציונלי לחיפוש תוצאות יד2 שכבר נגישות למנוע החיפוש. המועמדים ממתינים לאימות אדם ואינם נוספים אוטומטית להיסטוריה.

## גבולות הגרסה

זו תשתית עובדת לארכיון מצטבר, ולא ארכיון היסטורי ארצי מלא. אין כיום ספק נתוני עבר מחובר, ואין שחזור של מודעות שנמחקו ולא נותר להן תיעוד נגיש. אין סורק ארצי ליד2, איסוף פייסבוק, זיהוי לוחיות בתמונות או הורדת גלריות אוטומטית. החיפוש המובנה מסתמך על מספר רכב מפורש בטקסט, ולכן הכיסוי שלו עשוי להיות קטן. תמונות ומלל מלא מתקבלים דרך העלאה/ייבוא של תכנים שהעסק רשאי לשמור. הנתונים הממשלתיים שנצברו בסריקה הארצית נשארים במסך הק״מ הקיים; אינם משוכפלים לארכיון החדש.

## חיבור שירות חיפוש

להגדיר בשרת באופן פרטי `OPENAI_API_KEY`; לעולם לא בקוד דפדפן, בקובץ שנכנס לגיט או בצ׳אט. אפשר לבחור מודל נתמך באמצעות `ARCHIVE_SEARCH_MODEL`; ברירת המחדל היא `gpt-5.5`.

החיפוש משתמש ב־Responses API, כלי `web_search`, סינון `yad2.co.il`, `external_web_access:false` ו־`store:false`. רק כתובת מודעה שנכללה במקורות החיפוש ומלווה בקטע ראיה עם מספר הרכב יכולה להפוך למועמד. תוצאת מודל כשלעצמה אינה אימות של המודעה. נתונים לא מצוטטים מושמטים. המפתח אינו מחליף הרשאה להשתמש בתוכן מקור.

מגבלות הפיילוט: חיפוש אחד פעיל בכל פעם, 10 ניסיונות ב־24 שעות, רענון אותו רכב אחרי 6 שעות מחיפוש מוצלח. זמן המתנה לשירות עד 55 שניות. החיבור נבדק עם תשובות מדומות; יש לבדוק קריאה אמיתית, עלות וכיסוי לאחר הגדרת המפתח. אין קריאות בתשלום אוטומטיות ברקע.

תיעוד רשמי: https://developers.openai.com/api/docs/guides/tools-web-search

## שמירה וגיבוי

ברירת מחדל: `vehicle-archive.db` ותיקיית `vehicle-archive-media` ליד `DB_PATH`. אפשר להגדיר `VEHICLE_ARCHIVE_DB_PATH` לנתיב מוחלט בדיסק קבוע. בפריסה על Railway יש לוודא שהקבצים על ה־volume, לא בדיסק זמני של הפריסה.

לגיבוי עקבי יש להשתמש ב־SQLite backup API, או לעצור את השרת לפני העתקת בסיס הנתונים ותיקיית המדיה. אין להעתיק רק קובץ DB פעיל תוך התעלמות מ־WAL. יש להחריג את כל קובצי בסיס הנתונים ותיקיית המדיה מגיט.

לא נדרשות ספריות חדשות. ההרשאות מסתמכות על שער האימות הקיים ועל requireAdmin; עובדים מוגבלים אינם יכולים לפתוח את המסך או לקרוא את המדיה.

## מבנה ייבוא

עד 100 רשומות ועד 90KB בכל קובץ. כל הרשומות נבדקות לפני כתיבה, והכתיבה אטומית. אלה נתוני דוגמה מלאכותיים בלבד:

```json
{
  "records": [{
    "plate": "1234567",
    "source": "document",
    "sourceLabel": "דוח בדיקה — דוגמה בלבד",
    "sourceId": "example-report-2018",
    "evidenceDate": "2018-05-20",
    "dateKind": "document",
    "kilometers": 130000,
    "price": null,
    "year": 2010,
    "description": "תיעוד לדוגמה בלבד"
  }]
}
```

סוגי מקור בייבוא: `manual_listing`, `document`, `feed`. מקור ממשלתי אינו ניתן לבחירה בייבוא. סוגי תאריך: `published`, `updated`, `test`, `document`, `observed`. תאריך חסר: `evidenceDate:null` ו־`dateKind:"unknown"`. קישור HTTPS ב־`sourceUrl` משמש מזהה קבוע; בהיעדרו נדרשים `sourceId` ושם מקור.

## בדיקות

`node --test archive.test.js`

הבדיקות מכסות התמדה אחרי פתיחה מחדש, רכב משנת 2010 עם ראיה משנת 2018, גרסאות וכפילויות, כתיבה מקבילה, ייבוא אטומי, תאריך חסר, ירידה בק״מ, מקור חיפוש לא מצוטט, הרשאות, מקור בקשה, אישור מועמדים, הגבלת חיפושים ותמונות.

`preview-archive.js` הוא כלי תצוגה מקומית עם נתונים מלאכותיים בלבד, אינו מותקן במערכת ואסור לפרוס אותו.
